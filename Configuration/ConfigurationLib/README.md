# DotNetExtras.Configuration Library

The `DotNetExtras.Configuration` library makes it easier to read and transform configuration settings of .NET Core applications.

For details, check the topics under the `DotNetExtras.Configuration` namespace section in the [API documentation](https://alekdavis.github.io/dotnet-extras). For additional usage examples, see the [unit tests project](../ConfigurationLibTests).